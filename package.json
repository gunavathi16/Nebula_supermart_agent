{
  "name": "nebula-kirana-store",
  "version": "1.0.0",
  "description": "Indian Kirana Store POS & Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client && npm install --prefix bot",
    "dev": "concurrently -n \"SERVER,CLIENT,BOT\" -c \"cyan,green,yellow\" \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"node bot/bot.js\"",
    "bot": "node bot/bot.js",
    "bot:test": "node bot/test/agent.test.js",
    "seed": "node server/db/seed.js",
    "test": "node server/test/domain.test.js && node bot/test/agent.test.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
